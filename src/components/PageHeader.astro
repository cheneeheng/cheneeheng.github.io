---
interface Props {
  title: string;
  subtitle?: string;
}
const { title, subtitle } = Astro.props;
---

<header class="mb-10">
  <h1 class="text-3xl font-semibold tracking-tight">{title}</h1>
  {subtitle && <p class="mt-2 text-zinc-600 dark:text-zinc-300">{subtitle}</p>}
</header>
